apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "address-check-application.fullname" . }}-test-connection"
  labels:
    {{- include "address-check-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "address-check-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
