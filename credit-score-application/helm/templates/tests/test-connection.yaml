apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "credit-score-application.fullname" . }}-test-connection"
  labels:
    {{- include "credit-score-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "credit-score-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
